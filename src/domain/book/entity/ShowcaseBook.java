package domain.book.entity;

public class ShowcaseBook extends Book {
    public ShowcaseBook(String title, String author, String publisher, String isbn) {
        super(title, author, publisher, isbn);
    }
}
