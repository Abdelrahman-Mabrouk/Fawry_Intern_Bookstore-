package domain.book.entity;

public class PaperBook extends Book {

    public PaperBook(String title, String author, String publisher, String isbn) {
        super(title, author, publisher, isbn);
    }
}
